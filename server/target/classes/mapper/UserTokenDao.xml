<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.yomean.carbooking.dao.UserTokenDao">

    <insert id="addUserToken">
        insert into user_token(user_id, token, expire_time, update_time)
        values (#{userId}, #{token}, #{expireTime}, CURRENT_TIMESTAMP)
    </insert>

    <update id="updateUserToken">
        update user_token
        <set>
            <if test="token!=null">
                token = token,
            </if>
            <if test="expireTime!=null">
                expire_time = expireTime,
            </if>
            update_time = CURRENT_TIMESTAMP
        </set>
    </update>

    <delete id="deleteByUserId">
        delete
        from user_token
        where user_id = #{userId}
    </delete>

    <select id="getByUserId" resultType="com.yomean.carbooking.bean.UserToken">
        select id, user_id, token, expireTime, updateTime
        from user_token
        where user_id = #{userId}
    </select>

    <select id="getByToken" resultType="com.yomean.carbooking.bean.UserToken">
        select id, user_id, token, expireTime, updateTime
        from user_token
        where token = #{token}
    </select>

</mapper>